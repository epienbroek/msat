<?xml version='1.0' encoding='utf-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "../puppet-guide.ent">
%BOOK_ENTITIES;
]>
<section id="puppet_construction-puppet_lint">
  <title>Puppet lint RPM</title>

  <para>
<screen>
&t1p; <userinput>mkdir -p ~/puppet-puppet_lint/puppet-puppet_lint/etc/puppet/modules</userinput>
&t1p; <userinput>puppet module install puppetlabs-puppet_lint</userinput>
&t1p; <userinput>cp -R /etc/puppet/modules/stdlib ~/puppet-puppet_lint/puppet-puppet_lint/etc/puppet/modules</userinput>
&t1p; <userinput>ls ~/puppet-puppet_lint</userinput>
build.sh  puppet-puppet_lint  puppet-puppet_lint.spec
&t1p; <userinput>cat ~/puppet-puppet_lint/build.sh</userinput>
#!/bin/bash
RPM=puppet-puppet_lint
tar -cz --exclude=.git -f ~/rpm/SOURCES/${RPM}.tar.gz ${RPM}
rpmbuild --target noarch --clean -bb ${RPM}.spec
RC=$?
if [ $RC -ne 0 ]; then
  echo "ERROR: rpmbuild failed with exit code: $RC"
fi
rm ~/rpm/SOURCES/${RPM}.tar.gz
exit $RC

&t1p; <userinput>cat ~/puppet-puppet_lint/puppet-puppet_lint.spec</userinput>
Name:         puppet-puppet_lint
Version:      0.1.1
Release:      1
Summary:      Puppet gpgme
Group:        Applications/System
License:      GPL
Vendor:       MSAT
Source:       %{name}.tar.gz
BuildRoot:    %{_tmppath}/%{name}-root
Requires:     puppet

%description
This package contains the result of:
puppet module install puppetlabs-puppet_lint
mkdir -p ~/puppet-puppet_lint/puppet-puppet_lint/etc/puppet/modules
cp -R /etc/puppet/modules/stdlib \
    ~/puppet-puppet_lint/puppet-puppet_lint/etc/puppet/modules

%prep
%setup -q -n %{name}

%build
# Empty.

%install
rm -rf $RPM_BUILD_ROOT
mkdir $RPM_BUILD_ROOT
cp -R etc $RPM_BUILD_ROOT

%clean
rm -rf $RPM_BUILD_ROOT

%pre
# Empty.

%post
# Empty.

%preun
# Empty.

%postun
# Empty.

%files
%defattr(0644,root,root)
%dir /etc/puppet/modules/stdlib
/etc/puppet/modules/stdlib

%changelog
* Tue Jul 22 2014 Allard Berends &lt;allard.berends@example.com&gt; - 0.1.1-1
- Initial creation of the RPM
</screen>
  </para>

</section>
